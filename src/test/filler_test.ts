import { assert } from "chai";
import "mocha";

describe("Filler unit test", () => {
  it("should pass for now and be removed when real unit tests are created", () => {
    assert.isTrue(true);
  });
});
